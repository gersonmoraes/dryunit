(library
 ((name        ppx_dryunit)
  (public_name ppx_dryunit)
  (kind ppx_rewriter)
  (libraries (ocaml-migrate-parsetree ppx_dryunit_runtime ppx_tools ppx_driver))
  (preprocess (action (run ${bin:cppo} -V OCAML:${ocaml_version} ${<})))
))
